These invoices contain made-up names and amounts.
The Invoice register workflow reads invoice-*.txt from this folder.
Run Preview first. Then run Create outputs to write a new CSV and three copies.
The app writes all outputs under .local-data/exports. It never changes these inputs.
