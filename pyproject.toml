[project]
name = "localflow-studio"
version = "0.2.0"
description = "Build repeatable file workflows. Keep every step on your PC."
requires-python = ">=3.11"
license = "MIT"

[tool.black]
line-length = 100
target-version = ['py311']
include = '\.pyi?$'
